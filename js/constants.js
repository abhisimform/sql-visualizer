export const KEYWORDS = ["SELECT", "FROM", "WHERE", "GROUP BY", "HAVING", "ORDER BY", "LIMIT", "OFFSET"];

export const STEP_TO_KEYWORD_ID = {
  FROM: "FROM",
  WHERE: "WHERE",
  "GROUP BY": "GROUP_BY",
  HAVING: "HAVING",
  SELECT: "SELECT",
  ORDER_BY: "ORDER_BY",
  LIMIT: "LIMIT",
  OFFSET: "OFFSET"
};

export const STORAGE_KEYS = {
  query: "sql-visualizer-query",
  theme: "sql-visualizer-theme"
};
